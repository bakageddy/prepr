#include <bitset>
#include <string>
#include <vector>
using std::vector, std::string;

class Solution {
public:
	std::bitset<10> watch;
    vector<string> readBinaryWatch(int turnedOn) {
		watch;
    }
};
